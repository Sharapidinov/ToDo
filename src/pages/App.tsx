import { Layout } from "@/widgets/layout";
import { TaskList } from "@/widgets/main/task-list";

const App = () => {
    return (
        <Layout>
            <TaskList/>
        </Layout>
    );
};

export default App;